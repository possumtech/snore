{
	"type": "function",
	"function": {
		"name": "unknown",
		"description": "Register something you still need to find out.",
		"strict": true,
		"parameters": {
			"type": "object",
			"required": ["text"],
			"additionalProperties": false,
			"properties": {
				"text": {
					"type": "string",
					"minLength": 1,
					"description": "An open question that needs investigation."
				}
			}
		}
	}
}
