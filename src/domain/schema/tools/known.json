{
	"type": "function",
	"function": {
		"name": "known",
		"description": "Emit known facts, analysis, and conclusions as keyed entries. Required every turn.",
		"strict": true,
		"parameters": {
			"type": "object",
			"required": ["entries"],
			"additionalProperties": false,
			"properties": {
				"entries": {
					"type": "array",
					"description": "Key/value pairs. Knowledge keys use /:known/ prefix (lowercase slugs). File paths are bare relative paths.",
					"items": {
						"type": "object",
						"required": ["key", "value"],
						"additionalProperties": false,
						"properties": {
							"key": {
								"type": "string",
								"description": "Entry key. /:known/ prefix for knowledge, bare path for files."
							},
							"value": {
								"type": "string",
								"description": "The value to store."
							}
						}
					}
				}
			}
		}
	}
}
