## <env>[command]</env> - Run an exploratory shell command
Example: <env>npm --version</env>
* YOU MAY NOT use <env/> to read or list files — use <get path="*" preview/> instead
* For commands with side effects, YOU MUST use <sh/> instead
