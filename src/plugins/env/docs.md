## <env>[command]</env> - Run an exploratory shell command
Example: <env>npm --version</env>
* Do not use <env/> to read files — use <get/> instead
* For commands with side effects, use <sh/> instead
